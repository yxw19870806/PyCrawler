save.data格式（每一行代表一条记录，每条记录包含以下内容且用制表符tab键分割）：
	1.账号ID
	2.最后下载原创歌曲的ID（下载完成后自动记录，第一次使用不用填写）
	2.最后下载翻唱歌曲的ID（下载完成后自动记录，第一次使用不用填写）

账号ID获取方法：
	点击任意账号，进入主页
	地址类似为http://5sing.kugou.com/[abcdef]/default.html，其中[]内的字母就是账号的ID（实际不包括[]）
