save.data格式（每一行代表一条记录，每条记录包含以下内容且用制表符tab键分割）：
	1.账号ID
	2.最后下载图片的相册ID（下载完成后自动记录，第一次使用不用填写）

账号ID获取方法：
	点击任意账号，进入主页
	地址类似为http://[abcdef123].pp.163.com/，其中[]内的内容就是账号的ID（实际不包括[]）

配置参数（config.ini）：
	1.可配置下载指定数量的图片后结束程序，参数：GET_IMAGE_COUNT
	2.可配置下载指定数量的作品（1组作品包含多张图片）后结束程序，参数：GET_PAGE_COUNT
